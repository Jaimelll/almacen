<p id="notice"><%= notice %></p>
<p>
  <strong>Periodo:</strong>
  <%= @item.mmes %>
  <strong>Empresa:</strong>
  <%= @item.empresa %>
  <strong>Origen:</strong>
  <%= @item.origen %>
  <strong>Moneda:</strong>
   <%= @item.moneda %>
  <strong>Tc:</strong>
  <%= @item.tc %>

  </p>


<p>
  <strong>  Fecha: </strong>
  <%= @item.pfecha %>

  <strong>  Factura:</strong>
  <%= @item.serie %>-<%= @item.nfactu %>
  <strong>  Razon:</strong>
  <%= @item.client.razon %>

</p>





<div class="field">
  <h3> DETALLES</h3>


<table>
  <thead>
    <tr>

      <th>Descripcion</th>
      <th>Cantidad</th>
      <th>Precio</th>
      <th>Monto</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @item.details.each do |detail| %>
      <tr>
        <td><%= detail.descripcion %></td>
        <td><%= detail.cantidad %></td>
        <td><%= detail.precio %></td>
        <td><%= detail.monto %></td>
        <td><%= link_to 'Edit',edit_item_detail_path(@item,detail) %></td>
        <td><%= link_to 'Destroy', item_detail_path(@item,detail), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
   <p>
  </p>
  <h3><%= link_to '<Nuevo Detalle>', new_item_detail_path(@item) %> </h3>
</div>
<p>
  <strong>Subtotal</strong>
  <%= @item.subtotal %>
  <strong>Igv</strong>
  <%= @item.subtotal*0.18 %>
  <strong>Total</strong>
  <%= @item.subtotal*1.18 %>


  </p>



<%= link_to 'Edit', edit_item_path(@item) %> |
<%= link_to 'Back', items_path %>
